# portal — エージェント定義

## このリポジトリについて
ケヤキ開発チームの社内ポータル。Next.js アプリ + コンテンツ管理を一体化したリポジトリ。

## ディレクトリ構成

| ディレクトリ | 内容 |
|---|---|
| `app/` | Next.js App Router（ポータル本体） |
| `components/` | UI コンポーネント |
| `lib/` | ユーティリティ（documents.ts でファイル一覧取得） |
| `documents/` | 提案資料・仕様書・議事録（.md / .html） |
| `blog/` | note.com に投稿する記事の原稿（Markdown） |

## blog/ のルール
- ファイル名: `YYYY-MM-DD-タイトル.md`
- 1ファイル1記事
- 冒頭に frontmatter（title, date, tags）を付ける
- 読者は技術者ではない一般ユーザーも想定。わかりやすい日本語で書く

## documents/ のルール
- サブディレクトリでプロジェクト・案件ごとに整理する
- ファイル名は日本語でもOK（例: `portal-仕様書.md`）
- 提案資料は `proposals/`、仕様書は `specs/` に入れる

## コミットルール
- コミットメッセージは日本語でプレフィックス付き
  - 記事追加: `content: <タイトル>`
  - 資料追加: `docs: <ファイル名>`
  - 修正: `fix: <内容>`
- 1コミット1ファイルを原則とする（記事・資料単位）

## 開発コマンド

```bash
npm run dev    # 開発サーバー起動
npm run build  # ビルド確認（コミット前に必ず実行）
```

## 川上への注意
- ドキュメント追加は `documents/` に .md / .html を置くだけ（コード変更不要）
- ポータル機能変更時はコミット前に `npm run build` でビルドを確認する
- ユーザーから資料作成を依頼されたら `documents/` に保存してPRを作る
- 記事の執筆を依頼されたら `blog/` に下書きを保存してPRを作る
- 完了したら Slack でPRのURLを報告する
